const browserVersion ='chrome'

function getBrowserVersion(){
if(browserVersion.match('chrome')){
    let browserVersion 
    //console.log("Inside function and Inside block : "+browserVersion);
}
console.log("Inside function and Outside block : "+browserVersion);


}

getBrowserVersion();